{
	"schemaName": "Nucleus.Core",
  "version": "01.00.22",
  "operations": [
    {
      // in v1.4, we renamed layout footer panes to "FooterPane" to better reflect their usage
      "sql": {
        "sql": "UPDATE PageModules SET Pane = 'FooterPane WHERE Pane = 'BottomPane';"
      }
    }
  ]
}
